node_modules
.turbo
.next
out
dist
*.tsbuildinfo
pnpm-lock.yaml
next-env.d.ts

# Auto-généré, ne jamais formater manuellement — regénéré par `pnpm gen-types`
packages/supabase/src/types.ts

# Auto-généré par `eslint --suppress-all` / `--prune-suppressions` (garde-fou
# lint, voir .claude/rules/lint.md), dans le format JSON propre d'ESLint —
# non conforme aux règles Prettier. Ne jamais reformater avec `prettier
# --write` : `--prune-suppressions` réécrit ces fichiers dans le format ESLint
# après chaque lot de correction lint, ce qui romprait le formatage Prettier à
# chaque fois et ferait échouer `pnpm format:check` de façon récurrente.
**/eslint-suppressions.json

# SQL, markdown, brain — hors scope du formatage JS/TS
supabase/
.ibee-brain/
*.md

# Scripts de debug ponctuels committés (housekeeping séparé, pas ce lot)
scratch/
script.js
test_db.mjs

# Artefacts non-source (bundle décodé / export HTML) trackés à la racine —
# pas du code applicatif, ne doivent jamais être reformatés
.tmp-bundle-*.jsx
.tmp-profile-standalone.jsx
.tmp-standalone-decoded.html
IBEE Profil (offline).html
IBEE Profile (standalone).html
